name: json-validate
on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  json-yaml-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: json-validate
        id: json-validate
        uses: GrantBirki/json-yaml-validate@v4.0.0
